'use client';

// Top nav no longer duplicates sidebar links.
// Navigation lives in the sidebar only.
// This component is kept as an empty placeholder so the layout import doesn't break.
export function DashboardTopNav() {
  return null;
}
